Create a file called 1.7.2 containing the command you used to delete the snape branch (after merging the changes to main).

git checkout snape  
git push origin -d snape

and then we come back to main with :
git checkout main
